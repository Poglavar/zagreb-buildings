# Tables used by this repo

Synced from prod with `sync-tables zagreb` (prod DB "zagreb" on `do`).
Mark big tables `(schema-only)` if you don't need their data locally.

- public.zagreb_building
- public.zagreb_building_claim
